// import * as net from "net";

declare module "lark-websocket" {
    /*
    class LarkClient {
    }

    interface LarkWebsocket {
        ///**
         * Creates a websocket socket from a net.socket
         //
        createClient(socket: net.Socket): LarkClient;

        createNetServer(listener);
    }
    */
    const _: any;
    export = _;
}